extends ../layouts/error.pug

block config
    - var pageTitle = '401 - Access Denied';
    - var index = false;
    - var current_page = '401';

prepend css

block content

    // 401 Error
    .col-lg-6
        .text-center.mt-4
            h1.display-1 401
            p.lead Unauthorized
            p Access to this resource is denied.
            a(href='./')
                i.bi.bi-arrow-left.me-1
                | Return to Dashboard
