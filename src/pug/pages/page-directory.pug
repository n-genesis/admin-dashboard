extends ../layouts/dashboard.pug

block config
    - var pageTitle = 'Directory';
    - var index = false;
    - var current_page = 'directory';
    
prepend css

block content

  // page header
  include includes/page-header.pug

  include components/directory.pug

append scripts

