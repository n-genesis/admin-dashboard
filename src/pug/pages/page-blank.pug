extends ../layouts/dashboard.pug

block config
    - var pageTitle = 'Blank Page';
    - var index = false;
    - var current_page = 'blank-page';

prepend css

block content

    // page header
    include includes/page-header.pug

    // blank page
    p
        | The starting state of the menu will appear collapsed on smaller screens.
        | When toggled using the button above (mobile screens), the menu will open/close.
    p
        | Make sure to keep all page content within the
        code #main-content
        |. The top navbar is optional and can be removed. Just create an element with the
        code #sidebarToggle
        |ID which will toggle the menu when clicked.

append scripts

